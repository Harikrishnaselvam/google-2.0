/** @type {import('next').NextConfig} */
const nextConfig = {
    experimental:{
        appDir:true
    },
    images:{
        domains:['www.edigitalagency.com.au'],
    },
}

module.exports = nextConfig
